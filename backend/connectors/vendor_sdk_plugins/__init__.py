"""Approved INTEL-I vendor SDK adapters live in this package.

Each adapter must subclass connectors.vendor_sdk.adapter.VendorSDKAdapter and
be allowlisted with INTEL_I_SDK_MODULE_ALLOWLIST before it can be loaded.
"""
