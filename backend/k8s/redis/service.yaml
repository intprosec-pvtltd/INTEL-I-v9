apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: intel-i
  labels:
    app: redis
    app.kubernetes.io/part-of: intel-i
spec:
  selector:
    app: redis
  ports:
  - name: internal
    port: 6379
    targetPort: 6379
