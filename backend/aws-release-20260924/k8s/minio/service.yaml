apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: intel-i
  labels:
    app: minio
    app.kubernetes.io/part-of: intel-i
spec:
  selector:
    app: minio
  ports:
  - name: internal
    port: 9000
    targetPort: 9000
  - name: console
    port: 9001
    targetPort: 9001
