apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: intel-i
  labels:
    app: postgres
    app.kubernetes.io/part-of: intel-i
spec:
  selector:
    app: postgres
  ports:
  - name: internal
    port: 5432
    targetPort: 5432
